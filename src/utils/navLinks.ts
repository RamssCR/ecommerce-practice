/*
    Here we define the links for the navigation bar.
    The links are defined as a constant array of strings, 
    which can be used to generate the navigation bar dynamically.
*/
export const links = ['', 'products', 'contact'] as const